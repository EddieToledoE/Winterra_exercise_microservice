# Database Configuration
# Para MongoDB Atlas (recomendado):
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/winterra_exercise?retryWrites=true&w=majority

# Para MongoDB local:
# MONGODB_URI=mongodb://localhost:27017/winterra_exercise

# Server Configuration
PORT=3000
NODE_ENV=production

# JWT Configuration (for future authentication)
JWT_SECRET=your-super-secret-jwt-key
JWT_EXPIRES_IN=7d

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# CORS Configuration
CORS_ORIGIN=*

# Logging
LOG_LEVEL=info 